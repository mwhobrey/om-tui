## Summary

<!-- What changed and why, in a sentence or two. -->

## Test plan

- [ ] `go build ./...`
- [ ] `go test ./...`
- [ ] `go test -race ./...`
- [ ] Manually exercised the affected path in the TUI (not just unit tests), if this touches `internal/tui`, `internal/web`, or a bridge adapter
- [ ] Docs updated (`README.md`, `docs/`, `CLAUDE.md`) if behavior or setup steps changed

## Notes for the reviewer

<!-- Anything that needs context: risk areas, things deliberately left out, follow-ups. -->
